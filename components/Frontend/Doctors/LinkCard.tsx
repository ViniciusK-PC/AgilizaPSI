import Link from "next/link"

export default function LinkCard({className}:{className:string}) {
    return (
        <Link href="#" className={`flex gap-4 bg`}>
         <h2>Ansiedade</h2>
        </Link>
    )
}